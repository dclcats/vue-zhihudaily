
const config1 = {
	protocol: 'http://',
	common: 'news-at.zhihu.com/api/',
}

const config = {
	protocol: 'https://',
	site: 'test-zhihu.herokuapp.com/api'
}


// export const API_ROOT = config.protocol.concat(config.site)
// export const API_ROOT = config1.protocol.concat(config1.common)
export const API_ROOT = '/api'